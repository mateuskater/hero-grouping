#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct heroi{
    int index;
    int conf;
    int afin;
}heroi;

int main(){
    int itens, conflitos, afinidades;
    scanf("%d %d %d", &itens, &conflitos, &afinidades);

    for (int i = 0; i < conflitos + afinidades; i++){
        
    }
}